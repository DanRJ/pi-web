---
"@jmfederico/pi-web": patch
---

Keep the Modernist composer action controls on a single row on mobile (attach, model, thinking, Stop, Send) instead of wrapping Stop/Send to a second row; the model label truncates rather than reflowing.
