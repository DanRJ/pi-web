---
"@jmfederico/pi-web": patch
---

Make the Modernist assistant chat title non-sticky so it scrolls with the message instead of pinning to the top of the transcript (which detached it from the chat and overlapped the message on short mobile viewports). Replaces the occlusion approach from the previous fix.
