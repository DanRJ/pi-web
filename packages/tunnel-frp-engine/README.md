# @jmfederico/pi-web-tunnel-frp-engine

Small frp process helper used by the PI WEB Safe Tunnel connector. It is kept as a separate package so the connector can own tunnel supervision without importing PI WEB app internals.
